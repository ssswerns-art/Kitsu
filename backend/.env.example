APP_NAME=Kitsu Backend
DEBUG=false

SECRET_KEY=change-me

DB_HOST=db
DB_PORT=5432
DB_NAME=kitsu
DB_USER=kitsu
DB_PASSWORD=kitsu
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_RECYCLE=1800
DB_POOL_PRE_PING=true

DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ALLOWED_ORIGINS: Comma-separated list of allowed origins for CORS
# Supports both CSV format and JSON array format:
#   CSV:   http://localhost:3000,http://localhost:8080
#   JSON:  ["http://localhost:3000","http://localhost:8080"]
# IMPORTANT: Do NOT include trailing slashes in origins
#   Correct:   https://frontend-79rs.onrender.com
#   Incorrect: https://frontend-79rs.onrender.com/
# Note: Wildcard (*) is not allowed when credentials are used
ALLOWED_ORIGINS=http://localhost:3000
